import React from 'react';
import Header from '../header';

const Pricing = ()=>{
  return (<div>
            <Header title='Pricing' currentTab='pricing'/>
          </div>);
};

export default Pricing;
